// desaturation grid class
class desaturation {
    // Basic constructor
    constructor(myColor) {
        // Stored original color
        this.selected = myColor;
        
        // Array for all the calculated colors
        this.colors = new Array(13);
        for (var i = 0; i < colors.length; i++) {
            colors[i] = new Array(21);
        }
    }
    
    draw() {
         
    }
}
